name: StockSentinel CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend:
    name: Backend - Test
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run backend tests
        run: |
          # Replace with "pytest" once tests are added
          python -m compileall app

  frontend:
    name: Frontend - Lint & Build
    runs-on: ubuntu-latest
    needs: backend

    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

  docker:
    name: Build Docker images
    runs-on: ubuntu-latest
    needs: [backend, frontend]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        run: |
          docker build \
            -t stocksentinel-backend:${{ github.sha }} \
            ./backend

      - name: Build frontend image
        run: |
          docker build \
            -t stocksentinel-frontend:${{ github.sha }} \
            ./frontend

      # To push images to a registry, add a login step and push:
      # - name: Log in to registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      #
      # - name: Push images
      #   run: |
      #     docker tag stocksentinel-backend:${{ github.sha }} ghcr.io/<owner>/stocksentinel-backend:${{ github.sha }}
      #     docker tag stocksentinel-frontend:${{ github.sha }} ghcr.io/<owner>/stocksentinel-frontend:${{ github.sha }}
      #     docker push ghcr.io/<owner>/stocksentinel-backend:${{ github.sha }}
      #     docker push ghcr.io/<owner>/stocksentinel-frontend:${{ github.sha }}

